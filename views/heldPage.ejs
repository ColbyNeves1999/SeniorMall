<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Store Analysis</title>
    <link rel="stylesheet" href="/css/style.css" />
    <!-- Include Chart.js library -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>

<body>
    <section>
        <h1>Held Page
            <a href="/" class="btn">
                Home
            </a>
        </h1>
    </section>

    <div class="store-container">
        <ul style="list-style-type: square;">
            <% for(var i=0; i<heldList.length; i++) {%>
        
              <li style="margin-top: 5px;">
                <%= heldList[i].cartItemName %>
                <div>Requested: <%= heldList[i].quantity %></div>

                <form style="display: inline;" method="POST" action="/closeOrder">
                    <input type="hidden" id="cartItemId" name="cartItemId" value="<%=heldList[i].cartItemId%>">
                    <input name="fulfilled" id="fulfilled" class="fulfilled" placeholder="fulfilled" type="fulfilled" required>
                    <input type="hidden" id="storeName" name="storeName" value="<%=store.Name%>">
                    <button>Closed</button>
                </form>

              </li>
        
              <% } %>
          </ul>
    </div>

</body>
</html>
